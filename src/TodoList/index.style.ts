import styled from "styled-components";

const bodyWidth = "900px";

export const Layout = styled.div`
  margin: 2em auto;
  max-width: ${bodyWidth};
`;

export const Header = styled.div``;

export const ListSection = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
`;
